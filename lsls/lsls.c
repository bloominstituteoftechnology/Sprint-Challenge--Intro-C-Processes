#include <stdio.h>
#include <dirent.h>

/**
 * Main
 */
int main(int argc, char **argv)
{
  // Parse command line
  char *path = argv[1];
  // printf("Path: %d\n", argc);
  // printf("Path: %s\n", path);

  // Open directory

  // Repeatly read and print entries

  // Close directory

  return 0;
}